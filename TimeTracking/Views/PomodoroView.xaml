<UserControl x:Class="TimeTracking.Views.PomodoroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TimeTracking.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PomodoroViewModel}">
    <StackPanel Margin="24" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="{Binding Title}" FontSize="22" FontFamily="{StaticResource AppFontFamilyLight}" FontWeight="Normal" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding Message}" FontSize="14" Margin="0,8,0,0" HorizontalAlignment="Center" Opacity="0.7" />
    </StackPanel>
</UserControl>
